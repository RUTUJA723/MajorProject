<style>
  .btn-search{
    background-color: #fe424d;
    color: white;
    border-radius: 25px;
    padding: 0 1rem 0 1rem;
  }

  .btn-search:hover{
    background-color: #fe424d;
    color: white;
  }

  .btn-search i {
    display: inline;
    margin-right: 0.5rem;
  }

  .search-inp {
    border-radius: 25px;
    padding: 0.5rem 3rem 0.5rem 3rem;
  }
</style>

<nav class="navbar navbar-expand-lg bg-body-light border-bottom sticky-top shadow-sm">
  <div class="container">
    <a href="/listings/about" class="MRV navbar-brand fw-bold">
      <div class=" rounded-lg flex items-center justify-center mrv">
      <span class="text-primary-foreground font-display font-bold text-lg logo">MRV</span>
    </div>
    </a>

    <!-- <a class="navbar-brand" href="/home"><i class="fa-regular fa-compass"></i></a> -->
    <button 
    class="navbar-toggler" 
    type="button" data-bs-toggle="collapse" 
    data-bs-target="#navbarNav" 
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>

      <% if (currUser && currUser.isAdmin) { %>
  <div class="nav-item">
    <a class="nav-link" href="/admin/dashboard">Admin</a>
    </div>
<% } %>
      <div class="navbar-nav ms-auto ">
        <form class="d-flex" action="/listings/search" method="GET">
          <input class="form-control me-2" name="q" type="search" placeholder="Search Destination">
          <button class="btn btn-danger">Search</button>
        </form>
      <!-- <form class="d-flex" action="/listings" method="GET">
  <input 
    class="form-control me-2 search-inp" 
    type="search" 
    name="location"
    placeholder="Search destinations"
  >
  <button class="btn btn-search" type="submit">
    Search
  </button>
</form> -->

      </div>
      <div class="navbar-nav ms-auto">
        <li class="nav-item">
    <a class="nav-link" href="/wishlist">Wishlist ❤️</a>
</li>


<% if (typeof currUser) { %>
<li class="nav-item">
    <a class="nav-link" href="/bookings/mybookings">
        My Bookings
    </a>
</li>
<% } %>

        <a class="nav-link" href="/listings/new">Host Your Property</a>
        <% if(typeof currUser === "undefined" || !currUser){ %>
        <a class="nav-link" href="/signup"><b>Sign up</b></a>
        <a class="nav-link" href="/login"><b>Log in</b></a>
        <% } %>
         <% if(typeof currUser !== "undefined" && currUser){ %>
        <a class="nav-link" href="/logout">Log out</a>
        <% } %>
        <% if (currUser && !currUser.isHost && !currUser.owner && !currUser.isAdmin) { %>
    <% if (!currUser.hostRequest) { %>
        <a href="/become-host" class="btn btn-primary">Become a Host</a>
    <% } %>
<% } %>

      </div>
    </div>
  </div>
</nav>